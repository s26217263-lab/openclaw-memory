# 2026-03-27 Daily Notes

## 上午场（10:32）

### Watchdog 心跳
- 模型状态：GPT-5.4 可用，切换回 GPT-5.4
- 昨晚（2026-03-26）最后状态：晚上 21:00-22:46 主要在做神盘 Kickstarter 素材
- 昨天交付物：shenpan_landing_v2.html、compass_design_v1/v2.jpg、社交平台标准尺寸裁剪图 10 张、jimeng-auto-generator skill v2 更新（参考图增强流程）

### 待用户拍板（延续）
1. 1688 实拍图 → 已收到并裁剪 ✓
2. Kickstarter 账号注册 → 待定
3. 视频素材方向 → 待定
4. 神盘 Kickstarter Hero 图片 → 即梦生成中（21:51 触发），待确认结果

### 当前工作上下文
- 神盘方向已确认：赛波风水师定位，Kickstarter 欧美市场，定价 $29-$99
- jimeng-auto-generator skill 已可上传参考图到即梦，流程跑通
- ecompass task runner V2 骨架已有，但真实 ecompass 仍以聊天驱动为主
- watchdog / Gateway 稳定性已恢复

## 2026-03-27 12:24 CST — ecompass rescue, model preference, and automation fixes
- Rescued ecompass Kickstarter round-one delivery tasks 004–008 from false-complete state: rolled them back from done, repaired invalid/broken deliverables, added deliverable validation gates, and finished a PPT-style HTML delivery page at `ecompass/deliverables/kickstarter_round1_delivery.html` with local preview via `http://127.0.0.1:4173/deliverables/kickstarter_round1_delivery.html`.
- Learned and documented a hard rule in skills: if user cannot read `.md`, markdown is only internal material; final delivery must be human-facing HTML and must be verified in browser before claiming completion.
- User prefers `openai-codex/gpt-5.4` as the primary model, with `minimax/MiniMax-M2.7` only as fallback when GPT-5.4 is rate-limited. Implemented local monitoring/state mechanism in workspace (`scripts/check_gpt_availability.sh`, `HEARTBEAT.md`, launchd timer) to support GPT-first / MiniMax-fallback behavior.
- Fixed ecompass heartbeat execution environment issues caused by launchd PATH isolation (`env: node: No such file or directory`) by rewriting `ecompass/tasks/ecompass_heartbeat.sh` to use absolute paths and reloading `com.openclaw.ecompass-heartbeat.plist`.
- Created follow-up ecompass task `ecompass-009` for post-delivery operational actions (preview release, asset packaging, external-share confirmation, pre-publish checks). The LLM-generated draft failed validation due to timeout/`LLM ERROR`, so it was manually repaired into a valid deliverable and completed.
- Feishu file sending remains not fully workable through current `message.send` path: text sends succeed, but direct file sends hit schema/adapter behavior requiring `card`; Feishu wiki/drive also lacked ready writable targets for automatic upload.

## 2026-03-27 16:05 CST — 商业推进状态判断
- 用户明确认可当前方向：从救火式执行转向稳定的商业推进，认为这才算“可用的商业模型”。
- 用户要求记住这个状态，并确保按既有夜间备份逻辑同步到 GitHub，避免未来出问题时丢失这些阶段性认知与规则沉淀。

## 2026-03-27 21:06 CST — validation-lab 隔离方案确认
- 用户给出并确认了一版“市场验证 Agent 隔离落地方案 v1”，核心不是立刻安装 MiroFish，而是先把市场验证 agent 设计成独立、可控、可停、可替换的验证实验室。
- 核心原则已明确：职责隔离、环境隔离、工具可插拔、先定流程再装工具。MiroFish 未来只是 validation-lab 的可插拔模块，不是 agent 身份本身。
- 目标结构已明确：主 OpenClaw 系统之外，保留 `validation-lab` 作为独立市场验证实验室；其目录应包含 `inbox/`、`rounds/`、`reports/`、`tools/`、`logs/`、`state/`、`sandbox/`。
- round 机制已明确：该 agent 必须按 round 运作，输入包含项目名/验证目标/假设/目标用户/版本材料/成功失败标准，输出包含关键发现、接受点、误解点、风险点、修正建议以及是否进入下一轮或真实世界验证。
- 用户要求：先把这个方案保存下来，作为和市场验证 agent 强相关的长期设计基线；后续再基于此给出一个真正可用的 agent。
- 用户新增任务池规则：任务只要进入任务池，就默认继续推进，不要卡在 intake 等用户二次确认；除非出现真实 blocker，否则应直接推进到 done。
- 用户新增汇报偏好：任务池汇报时不要只写 `001/002/003` 这种编号，必须默认带上任务名和人话备注，降低理解成本。
- Watchdog 已重新接入 launchd plist：每 15 分钟检查一次，路径绝对路径，已装入 ~/Library/LaunchAgents/com.openclaw.watchdog.plist
- Healthcheck 已接入 OpenClaw cron：每天一次 security audit，cron id: 3df614d0，已配置 --every 1d
