## 2026-03-25 凌晨 watchdog 巡检

- 04:52 左右排查发现：`scripts/watchdog_openclaw.sh` 对 `openclaw health --json` 的解析有兼容性 bug。
- 现象：新版 health 输出只有顶层 `ok=true`，脚本把缺失的 `gateway.rpc.ok` 当成 `false`，导致健康状态被误判为失败，触发了多次不必要的 gateway restart。
- 已修复：仅当 `gateway.rpc.ok` 字段真实存在时才参与联合判断；否则只看顶层 `ok`。
- 已手动重置 `/tmp/openclaw-watchdog/fail_count` 为 0，并实测脚本返回 `Probe result: 1`，日志显示 `Gateway OK - reset fail count`。
- 当前状态：OpenClaw gateway 正常，watchdog 误重启问题已收敛。

- User asked to stabilize OpenClaw before deeper fixes. I inspected `openclaw status`, identified main instability as overly open Feishu group exposure and high-risk tool surface. Applied minimal hardening: changed `channels.feishu.groupPolicy` from `open` to `allowlist`, eliminating critical security audit findings related to open group execution.
- Applied second minimal hardening: set `channels.feishu.tools.doc = false` to reduce Feishu document permission exposure.
- User reported a remaining issue: Feishu sometimes delivers messages but the agent does not reply. Preliminary diagnosis: channel is connected and healthy, so likely causes are intermittent WebSocket reconnect windows, dedup/filtering behavior, or message-policy gating rather than full channel outage. Need future incident-time correlation (timestamp + DM/group + mention state + message text) to root-cause.

## 下午 session 续记（2026-03-25 晚）

### 备份策略
- 备份脚本：`/Users/palpet/.openclaw/workspace/scripts/nightly_openclaw_backup.sh`
- LaunchAgent：`~/Library/LaunchAgents/ai.openclaw.nightly-backup.plist`（每天 03:00 CST）
- 保留策略：只保留 7 天，超过 7 天的自动删除
- 备份目标目录：`/Users/palpet/.openclaw/workspace/backups/openclaw/`
- GitHub 同步层：尚未接通。目标仓库初定为 `s26217263-lab/openclaw-memory`，但待用户最终确认
- 注意：GitHub token `[REDACTED_EXPOSED_TOKEN]` 已暴露在聊天记录中，已建议用户 revocation

### agents 配置
- `ops` agent：绑定 ops 群 `oc_074cb1631c4e9dfc26e486fd17c7d32c`，agentId=ops
- `ecompass` agent：绑定罗盘群 `oc_a842b58b8108e7b3b44304e145c1b34d`，requireMention: false
- ecompass workspace 路径：`/Users/palpet/.openclaw/workspace`（与 main 共用）
- ecompass identity：`ecompass CEO`，emoji 🧭，theme: "CEO代理：少打扰，多推进；少空话，多证据；少请示，多负责。"

### ecompass CEO 角色定义（已持久化）
- 文件：`/Users/palpet/.openclaw/agents/ecompass/agent/CEO_PROFILE.md`
- 长期记忆：`/Users/palpet/.openclaw/agents/ecompass/memory/LONG_TERM.md`
- 核心原则：用户只看结果和关键拍板项，ecompass 默认主动推进
- 商业目标：电子罗盘项目争取做到 10 万美金级别收入；初期预算约 1 万人民币
- 模型策略：默认 gpt-5.4，备用 M2.7；不停工优先；gpt-5.4 受限/失败/挂起时立即切换 M2.7
- 汇报格式：当前目标 / 本次实际产出 / 证据 / 当前阻塞 / 需要用户拍板的点 / 下一步动作
- 证据模式：用户追问时只给文件路径、commit hash、repo/branch、页面路径等可核验信息
- 招人规则：临时调用/派生子 agent 默认允许；新增长期正式 agent 需用户拍板
- 闭环规则：老板发话后必须持续回应直到这轮问题闭环，不能只回一次就消失

### ecompass cron 任务（已创建）
- `ecompass-overnight-report`（ID: `2c72ff08-80d7-400d-ad38-0f18452d17c2`）：每天 07:00，汇报昨夜 23:00–07:00 工作进度
- `ecompass-hourly-day-report`（ID: `f3e8d515-634b-4a9d-90de-d783ee55e646`）：每天 09:00–19:00 每小时一次，推进情况汇报

### 当前使用模型
- main session：已切换为 `minimax/MiniMax-M2.7`（因 gpt-5.4 体感偏慢 15–20s）

### Feishu 状态
- 回复投递不稳定问题仍未彻底解决，属系统性
- 飞书已降级为辅助/通知渠道，不可靠时不要视为唯一沟通入口
- 建议主入口用 Control UI / Web
