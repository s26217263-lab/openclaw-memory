- 用户偏好：如果我未来遇到这种自己短时间解决不了的问题，不要闷头卡住，应直接来问用户，尽快补充关键约束或环境信息。
- 用户偏好：以后遇到问题，先用第一性原则拆解本质目标、约束和可替换层，再进入执行；不要一上来陷入表层实现。
- 用户偏好：如果 Codex/GPT-5.4 限速后系统自动降级到 MiniMax-M2.7，恢复后不要自动切回；先询问用户是否需要切回 GPT-5.4，因为该模型费用更高。
- 用户偏好：夜间睡觉时，如果系统健康正常，就不要主动发消息打扰；仅在异常/告警时通知。
- 用户偏好：watchdog/各cron任务要继续跑，但不要弹对话框；只在异常时告警。

## 2026-03-24 下午/晚间重要工作记录

### 用户核心诉求（反复强调）
- 用户要的是一个"能真正干活的AI助手"，不是"需要用户盯着、催着、替它做产品经理"的项目
- 核心原则："伤其十指不如断其一指"——先打完一个目标，再打下一个，不要每个都推一点
- 准点汇报规则：按整点10分节点（:10 / :20 / :30 / :40 / :50 / :00），不靠记忆，要机制化
- 遇到问题先自己想解法，可以开subagent并行，实在不行再来问用户

### 已拍板策略（所有主线）
- **即梦AI**：方案A（稳一点），不走激进路线；采用"本地队列 + 薄网页提交器"思路重构；今晚后面再集中测试
- **SearXNG**：降级为后续优化，短期用直接web search替代，不占主阻塞位
- **watchdog**：方案B（自动重启一次）；fail一次→重试→fail两次→restart→检查→还不行则停
- **腾讯云远程运维**：先不展开
- **OpenClaw稳定性**：本机优先，launchd + watchdog + 外部修复桥四层架构

### 当前优先级（已确认）
1. watchdog第一版落地（当前唯一目标）
2. OpenClaw稳定性收敛
3. 今晚后面再做即梦AI多条真实测试

### 第一性原则案例（用户指导）
- 即梦不该死磕"固定按钮"，而应把"本地队列"做稳定层，"网页提交器"做执行端
- SearXNG本质是搜索工具，不值得为"本地自建"牺牲主线进度；直接web search可替代
- 搜索工具可替代，本地SearXNG降级为后续优化

### 执行反思（用户批评）
- 最大的问题不是能力不够，而是执行方式：没有断一指、过早分散精力、汇报没机制化
- 用户要求：做完一个目标再打下一个，不要个个都推一点
- 以后遇到问题，可以开subagent并行，可以换路线，但不要只靠一个脑子死磕

### 环境状态
- Docker Desktop：已安装可用
- Python 3.11：已安装（/opt/homebrew/bin/python3.11）
- OpenClaw：Mac mini本地运行，gateway 127.0.0.1:18789
- 飞书消息发送：已知正确命令，target: ou_2369d90c137e41169ecf225ba3a7e356
- Jimeng页面：已登录，https://jimeng.jianying.com/ai-tool/generate?type=image&workspace=0；允许真实测试、允许并发提交、不用等生成完成、有排队也继续塞
- SearXNG容器：已启动运行在8080端口，但短期不阻塞主线

### 桌面STATUS.md位置
/Users/palpet/Desktop/STATUS.md

---

## 2026-03-24 晚间补充（22:31）

### Watchdog ✅ 完成
- Cron job: 每5分钟检查一次（job id: 7dd53373-8af4-462b-81c6-87c3805d82de）
- 策略：fail一次→等待30s重试，fail两次→restart网关→再检查，还不健康→发飞书告警
- 告警目标：ou_2369d90c137e41169ecf225ba3a7e356（用户本人）
- 飞书通知通道已验证正常（两次测试消息发送成功）
- 日志路径：/tmp/openclaw-watchdog.log

### OpenClaw 稳定性分析
- 主要故障：OpenAI Codex rate limit（ChatGPT Plus 账号，每小时约60分钟cooldown）
- 19:05-19:12：网络连接失败，连续4次全部超时（可能是 Clash 增强模式问题）
- 21:19-21:20：gpt-5.4 + gpt-5.4-mini 双双 rate limit，MiniMax-M2.7 正常
- 解决方案：MiniMax-M2.7 作为 primary，Codex 只作紧急 fallback
- 安全审计：3个 CRITICAL（groupPolicy="open" + elevated tools + fs 暴露），暂未处理
- 飞书偶发400错误（invalid receive_id），后验证通道正常

### 模型策略
- Primary：MiniMax-M2.7（API key，按量付费，无 per-hour 限制）
- Fallback：OpenAI Codex GPT-5.4 / GPT-5.4-mini（ChatGPT Plus 登录态，有消息数限制）
- 编程问题自动切换 Codex，日常用 MiniMax

### 即梦 AI 批量提交进展 ✅ 突破
- 关键发现：提交成功 = 输入框被清空，不需等生成完成
- 每次提交后立即检查：输入框清空→成功，立即填下一条；输入框还有文字→队列满/失败，等一下
- 即梦队列上限约3-4条，超过后新提交静默失败
- 5条测试提示词全部成功提交：
  1. 橘猫洗手台猫窝
  2. 布偶猫沙发睡觉
  3. 水墨画山水
  4. 开放式厨房
  5. 海边浪漫晚餐
- 浏览器标签页 targetId: 5764610F1D8090AE28079F61799A6104
- 输入框 ref: e575，提交按钮 ref: e613（每轮刷新后可能变化）
- 用户偏好：不用等生成，直接确认提交就下一条

### Watchdog 规则更新（22:43）
- Watchdog 每5分钟检查一次，异常才告警，正常不打扰
- 所有 Watchdog 回复使用**中文**
- 新增每小时整点汇报 cron job（hourly-status-report），直接发到用户飞书
